{
  "name": "social-hub",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/tauri.mjs dev",
    "build": "node scripts/tauri.mjs build",
    "build:app": "node scripts/tauri.mjs build --bundles app",
    "build:windows": "node scripts/build-windows.mjs",
    "check": "node --check src/main.js && cargo check --manifest-path src-tauri/Cargo.toml --offline"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2"
  }
}
